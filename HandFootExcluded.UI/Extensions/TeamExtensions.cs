using HandFootExcluded.Core.Teamservices;
using HandFootExcluded.Core.TeamServices;
using HandFootExcluded.UI.ViewModels;

namespace HandFootExcluded.UI.Extensions;

public static class TeamExtensions
{
    //public static ITeamViewModel ToViewModel(this ITeam team) =>
    //    new TeamViewModel
    //    {
    //        TeamName = team.Name,
    //        PlayerInitials = team.Player.Initials,
    //        PartnerInitials = team.Partner.Initials
    //    };

    //public static IEnumerable<ITeamViewModel> ToViewModel(this ITeams teams) => teams.Select(ToViewModel);
}